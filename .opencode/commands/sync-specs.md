---
description: Reconcile the AI-readable application and communication specs with the repository.
agent: data-maintainer
---

Read `AGENTS.md` and every file in `.opencode/specs/` first. Process this request: $ARGUMENTS

Inspect the repository sources of truth, then update only the specs that are stale, incomplete, or inconsistent with current behavior. Do not invent undocumented behavior. Report the sources used and the changes made.
